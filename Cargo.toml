[package]
name = "spiral-galaxy-map"
build = "build.rs"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14", features = ["bevy_dev_tools"] }
rand = "0.8.5"

[profile.dev.package."*"]
opt-level = 3

[profile.dev]
opt-level = 1
